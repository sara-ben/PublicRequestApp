import { Routes } from '@angular/router';
import { AddNewRequestComponent } from './add-new-request/add-new-request.component';

export const routes: Routes = [
	{ path: '', component: AddNewRequestComponent }
];
